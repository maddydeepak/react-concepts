JSX

• React.createElement vs JSX
• Benefits of JSX
• Behind the Scenes of JSX
• Babel & parcel role in JSX

Components

Functional Components

o Composing Components

Assignment

What is JSX?
• Superpowers of JSX

• Role of type attribute in script tag? What options can use there?

(TitleComponent) VS (<TitinComponent/> VS <TitleComponent></TitleComponent>) in JSX

Coding Assignment:

• Create a Nested header Element using React.createElement(h1, h2, h3 inside a div with class "title")

。 Create the same element using JSX

o Create a functional component of the same with JSX

o Pass attributes into the tag in JSX

o Composition of Component(Add a component inside another)

o TitleComponent) VS (<TitleComponent/> VS (TILLdComponents/stleComponent>) in JSX

Create a Header Component from scratch using Functional Components with JSX

o (TitleComponent) VS (<TitleComponent/>) VS

(<TitleComponent></TitleComponent>>) in JSX

Create a Header Component from scratch using Functional Components with JSX

o Add a Logo on left

• Add a search bar in middle

o Add User icon on right

o Add CSS to make it look nice

References

• Babel: https://babeljs.io/

• Attribute Type: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/script#attr-type

• JS Modules: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Modules:

• Babel Playground: https://babeljs.io/repl#

• React without JSX: https://reactjs.org/docs/react-without-jsx.html