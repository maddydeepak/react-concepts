import React from "react";

const useRestaurant = () => {
  return (
    <div>useRestaurant</div>
    // write api call for fetch restaurant using res ID and return .. this is custom hook
  );
};

export default useRestaurant;
